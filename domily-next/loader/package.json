{
  "name": "@domily/next-loader",
  "version": "0.0.0",
  "description": "Experimental document delivery, verification, and offline cache boundary for Domily Next",
  "type": "module",
  "exports": {
    ".": {
      "types": "./lib/types/index.d.ts",
      "import": "./lib/index.js"
    }
  },
  "files": [
    "lib"
  ],
  "scripts": {
    "build": "rm -rf ./lib && bun build ./src/index.ts --outdir ./lib --target browser --format esm --external @domily/next-ast && tsc -b ./tsconfig.types.json --force",
    "typecheck": "tsc -p ./tsconfig.json",
    "lint": "oxlint --config=../../.oxlintrc.json src test",
    "test": "bun test ./test"
  },
  "dependencies": {
    "@domily/next-ast": "workspace:^"
  },
  "license": "MIT"
}
