{
  "name": "@domily/next-codec-json",
  "version": "0.0.0",
  "description": "Experimental JSON codec for Domily Next documents",
  "type": "module",
  "exports": {
    ".": {
      "types": "./lib/types/index.d.ts",
      "import": "./lib/index.js"
    }
  },
  "files": [
    "lib"
  ],
  "scripts": {
    "build": "rm -rf ./lib && bun build ./src/index.ts --bundle --outdir ./lib --target browser --format esm --external @domily/next --external @domily/next/codec && tsc -b ./tsconfig.types.json --force",
    "typecheck": "tsc -p ./tsconfig.json",
    "lint": "oxlint --config=../../.oxlintrc.json src test",
    "test": "bun test ./test"
  },
  "peerDependencies": {
    "@domily/next": "workspace:^"
  },
  "license": "MIT"
}
