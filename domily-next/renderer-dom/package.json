{
  "name": "@domily/next-renderer-dom",
  "version": "0.0.0",
  "description": "Experimental safe DOM renderer adapter for Domily Next documents",
  "type": "module",
  "exports": {
    ".": {
      "types": "./lib/types/index.d.ts",
      "import": "./lib/index.js"
    }
  },
  "files": [
    "lib"
  ],
  "scripts": {
    "build": "rm -rf ./lib && bun build ./src/index.ts --outdir ./lib --target browser --format esm --external @domily/next-ast --external @domily/next-runtime && tsc -b ./tsconfig.types.json --force",
    "typecheck": "tsc -p ./tsconfig.json",
    "lint": "oxlint --config=../../.oxlintrc.json src test",
    "test": "bun test ./test"
  },
  "dependencies": {
    "@domily/next-ast": "workspace:^",
    "@domily/next-runtime": "workspace:^"
  },
  "license": "MIT"
}
